"use client";

/** Subtile Trennung zwischen Hero (slate-50) und Leistungen (weiß). */
export function SectionWave() {
  return (
    <div
      className="h-10 bg-gradient-to-b from-slate-50 to-white"
      aria-hidden
    />
  );
}
